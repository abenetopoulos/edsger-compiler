#include "stdio.h"

void main() {
    bool boolTest;
    int intTest;
    int* a;

    boolTest = 3 > 5 ? true : 10 > 5 ? true : false;

    if (boolTest) {
        writeString("correct\n");
    }else {
        writeString("incorrect\n");
    }

    a = new int;
    *a = 42 % 17;
    intTest = *a ? 3 * 7 : 4 * 9 ? true : false;

    a = new int * 5;
}
