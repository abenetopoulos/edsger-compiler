#include "stdio.h"

double globalDouble;

int test(int x) {
    int a;

    void anotherFunc(double b);

    void someFunc(int a) {
        int x;
        
        int someOtherFunc(bool b) {
            someOtherFunc(true);
            anotherFunc(24.2);
            
            return 42;
        }

        someOtherFunc(true);
        x = a + 42;
    }

    void anotherFunc(double b) {
        int f;
        
        f = 42;
        someFunc(f);
    }

    a = 42;
    someFunc(a);
    anotherFunc(42.1);
    return a;
}

void main() {
    int a;
    int i;

    void testFunc() {
        void someFunc(int a) {
            int x;
            x = a + 17;
        }
    }

    a = 17;
    testFunc();
    //someFunc(a);
    i = test(a);
}

