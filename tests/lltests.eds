#include "stdio.h"

double globalDouble;

int test(int x) {
    int a;

    void someFunc(int a) {
        int x;
        
        int someOtherFunc(bool b) {
            return -1; 
        }

        someOtherFunc(true);
        x = a + 42;
    }

    a = 42;
    someFunc(a);
    return a;
}

void main() {
    int a;
    int i;

    void testFunc() {
        void someFunc(int a) {
            int x;
            x = a + 17;
        }
    }

    a = 17;
    //testFunc();
    //someFunc(a);
    //i = test(a);
}

